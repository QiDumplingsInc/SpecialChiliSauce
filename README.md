# my_store

https://qidumplingsinc.github.io/SpecialChiliSauce/

## Testing

Behavioral tests for interactive JS (e.g. the newsletter signup form) run against the real `index.html` via jsdom, with network calls mocked.

```
npm install
npm test
```
